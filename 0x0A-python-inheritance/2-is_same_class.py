#!/usr/bin/python3
""" This module contains function which checks an object is an instance of a class"""


def is_same_class(obj, a_class):
    """ Checks whether an obj is an instance of a class"""
    if type(obj == a_class:
        return True
    return False
